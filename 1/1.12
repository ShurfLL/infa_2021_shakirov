import turtle
import math
turtle.tracer(2)
def circle(r):
    for i in range (1,181):
        turtle.forward(2*math.pi*r/360)
        turtle.left(1)
r1=50
r2=10
turtle.left(90)
for i in range (1,30):
    circle(r1)
    circle(r2)
    
