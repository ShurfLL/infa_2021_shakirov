import turtle
import math
#turtle.tracer(2)
def star(n, a):
	for i in range(1, n+1):
		turtle.forward(a)
		turtle.right(180.0*(1-1.0/n))
a=50
star(5, a)
turtle.penup()
turtle.forward(150)
turtle.pendown()
star(11, a)
